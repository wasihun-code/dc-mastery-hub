export default function MasteryMap() {
  return (
    <section className="rounded border border-dashed border-[var(--border)] bg-[var(--bg-card)] p-8">
      <h1 className="text-3xl font-bold text-[var(--text-primary)]">Mastery Map</h1>
      <p className="mt-3 text-[var(--text-muted)]">A visual map of concepts, dependencies, strengths, and weak spots.</p>
    </section>
  )
}
