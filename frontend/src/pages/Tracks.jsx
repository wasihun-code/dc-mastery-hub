export default function Tracks() {
  return (
    <section className="rounded border border-dashed border-[var(--border)] bg-[var(--bg-card)] p-8">
      <h1 className="text-3xl font-bold text-[var(--text-primary)]">My Tracks</h1>
      <p className="mt-3 text-[var(--text-muted)]">A catalog of your DataCamp data science learning tracks.</p>
    </section>
  )
}
