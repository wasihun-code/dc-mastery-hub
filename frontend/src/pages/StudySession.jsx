export default function StudySession() {
  return (
    <section className="rounded border border-dashed border-[var(--border)] bg-[var(--bg-card)] p-8">
      <h1 className="text-3xl font-bold text-[var(--text-primary)]">Study Session</h1>
      <p className="mt-3 text-[var(--text-muted)]">Focused practice sessions across flashcards, quizzes, challenges, and review loops.</p>
    </section>
  )
}
