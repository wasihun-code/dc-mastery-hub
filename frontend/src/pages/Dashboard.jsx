export default function Dashboard() {
  return (
    <section className="rounded border border-dashed border-[var(--border)] bg-[var(--bg-card)] p-8">
      <h1 className="text-3xl font-bold text-[var(--text-primary)]">Dashboard</h1>
      <p className="mt-3 text-[var(--text-muted)]">Your mastery overview, today's agenda, weak spots, and streak.</p>
    </section>
  )
}
