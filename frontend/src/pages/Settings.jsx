export default function Settings() {
  return (
    <section className="rounded border border-dashed border-[var(--border)] bg-[var(--bg-card)] p-8">
      <h1 className="text-3xl font-bold text-[var(--text-primary)]">Settings</h1>
      <p className="mt-3 text-[var(--text-muted)]">Content folder paths, preferences, and app configuration will live here.</p>
    </section>
  )
}
